<%= form_for(@comment) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div><%= hidden_field_tag "comment[entry_id]", @entry.id %></div>
  <%= f.text_area :content, placeholder: "What's your opinion?" %>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
